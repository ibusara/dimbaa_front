// import { createFeatureSelector, createSelector } from "@ngrx/store"
// import { PlanState } from "../reducers/plan.reducer"


// export const selectPlansState = createFeatureSelector<PlanState>("planState")

// export const isPlanRetrieving = createSelector(
//   selectPlansState,
//   (state: PlanState) => state.isFetching
// )

// export const getPlans = createSelector(
//   selectPlansState,
//   (state: PlanState) => state.plans
// )

// export const getCurrentPlan = createSelector(
//   selectPlansState,
//   (state: PlanState) => state.currentPlan
// )

// export const getCurrentPlanCheckpoints = createSelector(
//   selectPlansState,
//   (state: PlanState) => state.currentPlan.checkpoints
// )

// export const getCurrentPlanNotes = createSelector(
//   selectPlansState,
//   (state: PlanState) => state.currentPlan.notes
// )

// export const getCurrentPlanContingencies = createSelector(
//   selectPlansState,
//   (state: PlanState) => state.currentPlan.contingencies
// )
